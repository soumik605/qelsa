import React from 'react'
import { JobComparisonPage } from '../components/JobComparisonPage'

const JobCompare = () => {
  return (
    <Layout activeSection={"jobs"}>
      {/* <JobComparisonPage jobs={comparedJobs} onBack={handleBackFromComparison} onRemoveJob={handleRemoveJobFromComparison} /> */}
    </Layout>
  )
}

export default JobCompare