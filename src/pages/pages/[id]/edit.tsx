import { CompanyPageEditor } from "@/components/CompanyPageEditor";
import Layout from "@/layout";

const Edit = () => {
  return (
    <Layout activeSection={"pages"}>
      <CompanyPageEditor />;
    </Layout>
  );
};

export default Edit;
